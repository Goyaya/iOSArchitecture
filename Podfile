# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'iOS Knowledge Architecture' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for iOS Knowledge Architecture
  pod 'DoraemonKit/Core', '~> 1.1.6', :configurations => ['Debug']
  pod 'LookinServer', :configurations => ['Debug']
  pod 'Masonry'
  target 'iOS Knowledge ArchitectureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS Knowledge ArchitectureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
